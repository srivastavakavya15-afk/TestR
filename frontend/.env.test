# jsdom's fetch cannot resolve a relative base URL, so tests need an absolute origin. It must match
# `test.environmentOptions.jsdom.url` in vite.config.ts, otherwise MSW's relative-path handlers
# resolve against a different origin than the client requests.
VITE_API_BASE_URL=http://localhost:5173
